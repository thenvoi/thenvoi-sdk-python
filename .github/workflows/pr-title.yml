name: PR Title

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

permissions:
  pull-requests: read

jobs:
  validate:
    name: Validate PR Title
    runs-on: ubuntu-latest
    steps:
      - name: Validate Conventional Commits format
        uses: amannn/action-semantic-pull-request@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          # Configure which types are allowed
          types: |
            feat
            fix
            docs
            style
            refactor
            perf
            test
            build
            ci
            chore
            revert
          # Configure which scopes are allowed (optional)
          # Aligned with issue template components
          scopes: |
            sdk
            agent
            api
            websocket
            integrations
            config
            docs
            ci
            perf
            deps
          # Require scope to be provided
          requireScope: false
          # Disable validation for merge commits
          ignoreLabels: |
            dependencies
            automated
          # Error message shown when validation fails
          subjectPattern: ^.{1,80}$
          subjectPatternError: |
            PR title must be 80 characters or less.

            Expected format: type(scope): description
            Example: feat(agent): add event streaming support

            See CONTRIBUTING.md for details.
